import type { AgentNode } from "../state.js";

/**
 * gatherNews — STUB (real impl: P2 step 4).
 *
 * Will run the P1 retrieval loop (searchEvidence → indexEvidence → retrieve) for the market
 * question and append the grounded passages to `state.news`. Pass-through for now so the whole
 * graph is runnable end to end.
 */
export const gatherNews: AgentNode = async (_state) => {
	return {};
};
