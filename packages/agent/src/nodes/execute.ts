import type { AgentNode } from "../state.js";

/**
 * execute — STUB (real impl: P2 step 7).
 *
 * Will fill the approved decision as a paper trade at the best ask → a `Position` (shares, cost,
 * units). Writes `state.position`. No-op when the decision was no-bet. Pass-through for now.
 */
export const execute: AgentNode = async (_state) => {
	return {};
};
