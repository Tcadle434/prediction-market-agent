# dependencies
node_modules/
.pnpm-store/

# build output
dist/
*.tsbuildinfo

# generated Prisma 7 client (run `pnpm --filter @lykos/rag db:generate` to (re)create)
# biome.json vcs.useIgnoreFile=true → Biome also skips it
packages/rag/src/generated/

# env / secrets — never commit real keys
.env
.env.local
.env.*.local

# test / coverage
coverage/

# python (eval stats + plots live here later)
__pycache__/
.venv/
*.pyc

# caches (fetched markets/evidence get cached here so we don't re-hit APIs)
.turbo/
data/cache/

# logs
*.log

# os
.DS_Store

# Generated real-market snapshot for the live-agent eval (regenerate with fetch-markets.mjs)
eval/data/live_markets.json
